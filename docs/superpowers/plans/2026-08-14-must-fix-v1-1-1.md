# Must-fix v1.1.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Fix the six release blockers without expanding product scope.

**Architecture:** Keep immutable evidence hashes at every boundary. Share correction auditing between checkpoint, review, and finalization; share media/runtime fingerprints between preparation checkpoints and final provenance; keep recovery artifacts in the runtime job directory.

**Tech Stack:** Python 3.11-3.13 standard library, Windows PowerShell 5.1, JSON/JSONL, Markdown, GitHub Actions.

---

### Task 1: Make correction audit remediable and reviewable

**Files:**
- Modify: `scripts/correction_contract.py`
- Modify: `scripts/checkpoint_corrections.py`
- Create: `scripts/review_corrections.py`
- Modify: `scripts/finalize_transcript.py`
- Modify: `tests/test_correction_contract.py`
- Modify: `tests/test_finalize_transcript.py`

- [ ] Add failing tests for hash-guarded replacement, deterministic finding IDs, finding-to-clip mapping, hash-bound durable reviews, and rejection of a bare global acknowledgement.
- [ ] Implement atomic replacement while preserving exact raw pairing and job locking.
- [ ] Implement review listing/recording and require current confirmed findings in finalization.
- [ ] Run focused correction/finalizer tests.

### Task 2: Verify runtime and bind checkpoints to media

**Files:**
- Modify: `scripts/prepare_transcript.py`
- Modify: `scripts/finalize_transcript.py`
- Modify: `tests/test_prepare_transcript.py`
- Modify: `tests/test_finalize_transcript.py`

- [ ] Add failing tests for tampered runtime artifacts, paths outside the runtime root, same-duration replacement WAVs, stale clips/checkpoints, and audio hashes in formal provenance.
- [ ] Re-hash runtime executables/models before use and reject escaped paths.
- [ ] Bind VAD, clips, and segment checkpoints to a deterministic media fingerprint.
- [ ] Persist and render source-audio and normalized-WAV hashes.
- [ ] Run focused preparation/finalizer tests.

### Task 3: Recover caches and fail early on output roots

**Files:**
- Modify: `scripts/prepare_transcript.py`
- Modify: `tests/test_prepare_transcript.py`

- [ ] Add failing tests proving corrupt metadata/VAD/media are quarantined and recomputed, and output-root failure occurs before any runner call.
- [ ] Add generic unique quarantine moves without deleting evidence.
- [ ] Create and probe the output root before metadata/download/transcription.
- [ ] Run focused preparation tests.

### Task 4: Protect Windows runtime storage

**Files:**
- Create: `scripts/runtime_acl.ps1`
- Modify: `scripts/bootstrap_runtime.ps1`
- Create: `tests/test-runtime-acl.ps1`
- Modify: `tests/static-contract.ps1`

- [ ] Add a failing ACL test showing inherited broad access remains on a fresh directory.
- [ ] Apply a protected DACL for current user, SYSTEM, and Administrators before runtime use and verify the resulting access rules.
- [ ] Run ACL, runtime-layout, and static PowerShell tests.

### Task 5: Make installation and runtime-root instructions executable

**Files:**
- Modify: `SKILL.md`
- Modify: `README.md`
- Modify: `tests/static-contract.ps1`

- [ ] Add failing static checks for the stock installer’s required root path/name and one runtime root carried through both commands.
- [ ] Update the minimal Skill workflow and public README while preserving `点点关注谢谢喵` as the exact first line.
- [ ] Remove the overstated all-stage resume claim.
- [ ] Run static checks and Skill validation.

### Task 6: Release verification

- [ ] Run all Python and PowerShell tests, runtime asset verification, Skill validation, and `git diff --check`.
- [ ] Review the diff against this six-item boundary and commit the feature branch.
- [ ] Merge into `main`, push GitHub, and confirm the GitHub Actions run succeeds.
